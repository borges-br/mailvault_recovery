<ResourceDictionary xmlns="https://github.com/avaloniaui"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <!-- ============================================================
         Superfícies e elementos reutilizáveis
         ============================================================ -->

    <!-- ===== Card — Border de superfície padrão ===== -->
    <ControlTheme x:Key="Card" TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource BgSurfaceBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource BorderSubtleBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="8"/>
        <Setter Property="Padding" Value="16"/>
        <Setter Property="BoxShadow" Value="0 1 2 0 #66000000"/>
    </ControlTheme>

    <!-- Card elevado (hero / destaque) -->
    <ControlTheme x:Key="CardElevated" TargetType="Border" BasedOn="{StaticResource Card}">
        <Setter Property="Background" Value="{DynamicResource BgElevatedBrush}"/>
        <Setter Property="CornerRadius" Value="12"/>
        <Setter Property="BoxShadow" Value="0 6 20 0 #73000000"/>
    </ControlTheme>

    <!-- ===== Badge — ContentControl com variantes de cor =====
         Default = neutral. Classes: accent/success/warning/danger/info/exp -->
    <ControlTheme x:Key="Badge" TargetType="ContentControl">
        <Setter Property="FontFamily" Value="{StaticResource FontUI}"/>
        <Setter Property="FontSize" Value="10"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="Foreground" Value="{DynamicResource TextMutedBrush}"/>
        <Setter Property="Template">
            <ControlTemplate>
                <Border Name="PART_Badge"
                        Background="{DynamicResource BgElevatedBrush}"
                        BorderBrush="{DynamicResource BorderSubtleBrush}"
                        BorderThickness="1"
                        CornerRadius="4"
                        Padding="8,3">
                    <ContentPresenter Content="{TemplateBinding Content}"
                                      ContentTemplate="{TemplateBinding ContentTemplate}"
                                      Foreground="{TemplateBinding Foreground}"
                                      VerticalAlignment="Center"
                                      HorizontalAlignment="Center"/>
                </Border>
            </ControlTemplate>
        </Setter>

        <!-- variantes -->
        <Style Selector="^.accent">
            <Setter Property="Foreground" Value="{DynamicResource AccentBrush}"/>
            <Style Selector="^ /template/ Border#PART_Badge">
                <Setter Property="Background" Value="{DynamicResource AccentGlowBrush}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource AccentLineBrush}"/>
            </Style>
        </Style>
        <Style Selector="^.success">
            <Setter Property="Foreground" Value="{DynamicResource SuccessBrush}"/>
            <Style Selector="^ /template/ Border#PART_Badge">
                <Setter Property="Background" Value="{DynamicResource SuccessSubtleBrush}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource SuccessBrush}"/>
            </Style>
        </Style>
        <Style Selector="^.warning">
            <Setter Property="Foreground" Value="{DynamicResource WarningBrush}"/>
            <Style Selector="^ /template/ Border#PART_Badge">
                <Setter Property="Background" Value="{DynamicResource WarningSubtleBrush}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource WarningBrush}"/>
            </Style>
        </Style>
        <Style Selector="^.danger">
            <Setter Property="Foreground" Value="{DynamicResource DangerBrush}"/>
            <Style Selector="^ /template/ Border#PART_Badge">
                <Setter Property="Background" Value="{DynamicResource DangerSubtleBrush}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource DangerBrush}"/>
            </Style>
        </Style>
        <Style Selector="^.info">
            <Setter Property="Foreground" Value="{DynamicResource InfoBrush}"/>
            <Style Selector="^ /template/ Border#PART_Badge">
                <Setter Property="Background" Value="{DynamicResource InfoSubtleBrush}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource InfoBrush}"/>
            </Style>
        </Style>
        <Style Selector="^.exp">
            <Setter Property="Foreground" Value="{DynamicResource ExperimentalBrush}"/>
            <Style Selector="^ /template/ Border#PART_Badge">
                <Setter Property="Background" Value="{DynamicResource ExperimentalSubtleBrush}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource ExperimentalBrush}"/>
            </Style>
        </Style>
    </ControlTheme>

    <!-- ===== StatusDot — Ellipse 7x7 colorida ===== -->
    <ControlTheme x:Key="StatusDot" TargetType="Ellipse">
        <Setter Property="Width" Value="7"/>
        <Setter Property="Height" Value="7"/>
        <Setter Property="Fill" Value="{DynamicResource TextMutedBrush}"/>
        <Style Selector="^.accent"><Setter Property="Fill" Value="{DynamicResource AccentBrush}"/></Style>
        <Style Selector="^.success"><Setter Property="Fill" Value="{DynamicResource SuccessBrush}"/></Style>
        <Style Selector="^.warning"><Setter Property="Fill" Value="{DynamicResource WarningBrush}"/></Style>
        <Style Selector="^.danger"><Setter Property="Fill" Value="{DynamicResource DangerBrush}"/></Style>
        <Style Selector="^.info"><Setter Property="Fill" Value="{DynamicResource InfoBrush}"/></Style>
        <Style Selector="^.exp"><Setter Property="Fill" Value="{DynamicResource ExperimentalBrush}"/></Style>
    </ControlTheme>

    <!-- ===== NavButton — item de navegação da sidebar ===== -->
    <!-- Mantém o modelo de Command existente (não usa seleção de ListBox).
         Classe "active" marca o módulo atual com barra esquerda cyan. -->
    <ControlTheme x:Key="NavButton" TargetType="Button">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{DynamicResource TextMutedBrush}"/>
        <Setter Property="FontFamily" Value="{StaticResource FontUI}"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="FontWeight" Value="Medium"/>
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="Margin" Value="0,1"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <ControlTemplate>
                <Border Name="PART_Root"
                        Background="{TemplateBinding Background}"
                        CornerRadius="6">
                    <Grid ColumnDefinitions="3,*">
                        <Border Name="PART_Bar" Grid.Column="0"
                                Width="3" CornerRadius="2"
                                Margin="0,4" Background="Transparent"/>
                        <ContentPresenter Grid.Column="1"
                                          Content="{TemplateBinding Content}"
                                          Foreground="{TemplateBinding Foreground}"
                                          Padding="11,11"
                                          VerticalAlignment="Center"
                                          HorizontalAlignment="Left"/>
                    </Grid>
                </Border>
            </ControlTemplate>
        </Setter>

        <Style Selector="^:pointerover /template/ Border#PART_Root">
            <Setter Property="Background" Value="{DynamicResource BgElevatedBrush}"/>
        </Style>
        <Style Selector="^:pointerover">
            <Setter Property="Foreground" Value="{DynamicResource AccentBrush}"/>
        </Style>

        <!-- estado ativo -->
        <Style Selector="^.active /template/ Border#PART_Root">
            <Setter Property="Background" Value="{DynamicResource AccentGlowBrush}"/>
        </Style>
        <Style Selector="^.active /template/ Border#PART_Bar">
            <Setter Property="Background" Value="{DynamicResource AccentBrush}"/>
        </Style>
        <Style Selector="^.active">
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

        <Style Selector="^:disabled">
            <Setter Property="Opacity" Value="0.45"/>
        </Style>
    </ControlTheme>

    <!-- ===== ProgressBarStripe — barra com gradiente accent ===== -->
    <ControlTheme x:Key="ProgressBarStripe" TargetType="ProgressBar" BasedOn="{StaticResource {x:Type ProgressBar}}">
        <Setter Property="Background" Value="{DynamicResource BgInputBrush}"/>
        <Setter Property="MinHeight" Value="10"/>
        <Setter Property="CornerRadius" Value="6"/>
        <Setter Property="Foreground">
            <Setter.Value>
                <LinearGradientBrush StartPoint="0%,0%" EndPoint="100%,0%">
                    <GradientStop Offset="0" Color="{DynamicResource AccentStrongColor}"/>
                    <GradientStop Offset="1" Color="{DynamicResource AccentColor}"/>
                </LinearGradientBrush>
            </Setter.Value>
        </Setter>
    </ControlTheme>

</ResourceDictionary>
